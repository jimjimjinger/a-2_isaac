# Define reward terms for rover driving stability training.
#
# TODO(real RL training):
# - Define reward terms for target progress, collision avoidance, smooth control,
#   energy use, terrain safety, and mission completion.
# - Keep the reward function separate from ROS2 runtime nodes so training can
#   run offline or inside Isaac Sim without changing the live communication API.
